{
  "name": "esbuild-typescript-minimal-boilerplate",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "nodemon",
    "build": "esbuild src/index.ts --bundle --platform=node --outdir=dist --minify",
    "start:build": "node dist"
  },
  "devDependencies": {
    "nodemon": "^2.0.16",
    "prettier": "^2.6.2"
  }
}
